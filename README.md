# BlokBird

BlokBird 是浏览器里的交互式手机界面设计工具。选择组件或应用流程模板，在手机画布中调整布局、样式、动画与点击行为，同时查看对应的 HTML、CSS 和 JavaScript。当前处于持续开发阶段，尚未发布正式版本。

编辑器默认配色使用米白 `#F5F3EF`、墨绿 `#2F6B4F`、少量琥珀黄 `#D9A441` 与深色文字 `#1F2A24`。用户仍可在属性栏自由修改作品内元素的颜色。

## 运行

直接用浏览器打开 `index.html`，也可以把整个目录放到任意静态网站服务。应用资源均使用相对路径，无需安装依赖或构建。建议使用最新版 Chrome、Edge 或 Firefox。

## 已有功能

- 自由新建、重命名、复制和删除页面；可将页面设为底部主导航或详情子页面。7 套模板各生成 3 个相连页面，可预览从入口到结果的流程
- 固定高度的手机画布，内容在手机内部滚动；编辑器左右栏撑满浏览器高度并各自滚动
- 底部导航可选 12 个原创 SVG 图标，也可上传 SVG、PNG、JPG、WEBP；支持调整图标大小、线条及选中颜色
- 109 个组件与素材预设，覆盖标题、文本、按钮、图片、卡片、分隔线、布局容器、输入框与开关
- 26 张本项目原创 SVG 图像，并可导入自己的 PNG、JPG、WEBP 图片；上传前检查文件内容、体积与解码结果
- 双栏、三栏、纵向、主次分栏与内容面板；组件可移入布局并调整顺序
- 按钮默认、悬停、按下和禁用状态；可设置提示、弹窗、页面跳转或链接。表单跳转可检查输入非空与格式；输入框和开关在预览及导出网页中可操作
- 10 种动画，可调整触发方式、时长、延迟与运动节奏；支持重播
- 阴影、发光、玻璃质感、渐变与描边等视觉效果
- 选中元素时查看 HTML、CSS、JavaScript 片段
- 自动保存、撤销与重做、项目 JSON 导入导出，以及可独立运行的 HTML 导出

## 自由创作

从左侧选择组件，点击预设放入当前页面。选中手机上的元素可在右侧修改属性；拖动可调整顺序或放入布局容器。点击“预览交互”体验按钮与页面跳转。模板会加入一组相连页面，并保留已有内容。页面列表右键可切换主导航与子页面类型；子页面有返回按钮，也不会占用底部导航。

## 开发检查

项目使用原生 HTML、CSS 和 JavaScript。Node.js 20 或更新版本可运行项目测试：

```bash
npm test
```

测试覆盖预设、旧项目导入、布局操作、应用流程、动画、图片上传与导出网页。代码无需打包，`index.html` 可以直接作为 GitHub Pages 的站点入口。

## GitHub 准备状态

本地 Git 仓库已经建立，尚未添加远程仓库或上传。正式公开前仍需完成视觉验收、浏览器操作检查，并决定开源许可证。发布时将本目录作为仓库根目录；在 GitHub Pages 中选择主分支根目录即可获得公开网址。

项目数据保存在浏览器的 `localStorage` 中，建议定期下载项目 JSON 文件备份。上传的图片以 data URL 保存，图片较多时应留意浏览器存储空间。
